1. 让专业的人做专业的事情
	i. ChatGPT -> 聊天机器人 -> 大語言模型 -> 聊天生成預訓練轉換器
	ii. 文本分类器 -> 朴素贝叶斯分类器 -> 分类模型 -> 低纬度的向量数据
2. 加强学习->强化学习->更新数据集->重画向量图
	i. 采取主动学习策略（用户反馈调整）
3. 根据应用场景采用合适的训练模型。
4. 本地部署/云AI
5. 文本分类器：
    -> 文字判断： e.g real_name -> name; mingzi -> name
    -> 用户反馈储存: 用户可以说出 contactFirstName != name; contactFirstName = FirstName
    -> 再训练: 根据客户反馈调整向量数据
    -> 储存模型;持久化储存模型
6. 训练集:
    -> 避免重复和相似训练-> 向量噪点 -> 降低准确
    -> 数据边界,模糊值准确定义
    -> 训练集%70, 验证集（从训练集抽出验证）, 测试集%30
    -> 超参数调优 -> 自行摸索 -> X
总结： 判断自己做。o.o
